import { makeStyles } from '@material-ui/styles';

const useStyles = makeStyles((theme) => ({
  container: {
    height: '100%',
    width: '100%',
    position: 'relative',
    zIndex: 1
  },
  containerMap: {
    height: '100%',
    width: '100%',
  }
}));

export default useStyles;